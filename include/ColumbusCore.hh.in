#ifndef COLUMBUSCORE_H
#define COLUMBUSCORE_H

#include <cstdlib>
#include <stdint.h> // Change to <cstdint> once we go c++0x. GCC will not permit it earlier.

#ifdef DEBUG_MESSAGES
#include<cstdio>
#define debugMessage(...) printf(__VA_ARGS__);
#else
#define debugMessage(...)
#endif


#define COLUMBUS_VERSION_STRING "${SO_VERSION}"
#define COLUMBUS_ABI_VERSION ${ABI_VERSION}

#define COL_ASSERT(value) if(!(value)) exit(127);

typedef ${LETTER_TYPE} Letter;
#define INTERNAL_ENCODING "${INTERNAL_ENCODING}"

typedef size_t WordID;
#define INVALID_WORDCODE SIZE_MAX

#endif
